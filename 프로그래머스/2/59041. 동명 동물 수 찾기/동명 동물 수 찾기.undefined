-- 코드를 입력하세요
SELECT NAME, count(*) as COUNT
from ANIMAL_INS
where NAME IS NOT NULL
group by NAME
having count(*) >= 2
order by NAME;

-- 동물 보호소에 들어온 동물 이름 중 두 번 이상 쓰인 이름과 해당 이름이 쓰인 횟수를 조회
--  이름이 없는 동물은 집계에서 제외
-- 이름 순